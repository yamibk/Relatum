# Relatum「研究」工作区接力开发

> 状态：M1 已完成，M2 尚未开始。最后校准：2026-09-19。
> 新对话提示词：`读《研究工作区接力开发.md》，继续开发并更新进度。`

## 0. AI 接手流程

1. 完整读取根目录 `AGENTS.md` 与本文；源码与本文冲突时以源码为准。
2. 运行 `git status --short`，保护用户已有改动和全部用户数据。
3. 只做本文“当前下一步”，先定位最小改动面，再实现、验证。
4. 结束前更新本文的“当前快照”和“当前下一步”，不要追加流水账。
5. 若实现改变了真实入口、数据位置、行为或验证方式，同步更新 `AGENTS.md` 对应章节。

本文是可修改的路线图，不是冻结规格。开发中允许重做研究工作区、改变研究数据结构、删除未完成的研究功能；兼容旧的**研究实验数据**不是目标。任何时候都不得连带删除或改写原画布、笔记、学习等用户数据。

## 1. 最终目标

把起步页现有的空白【研究】工作区变成独立的可执行无限画布：节点不仅展示内容，还能通过连线传递数据，完成数学运算、逻辑判断、计时、拓扑分析和受控的节点操作，逐步接近 Graphif 自动计算引擎的能力。

最小示例：

```text
[5] ─┐
     ├─> [加法] ─> [11]
[6] ─┘
```

目标能力按层次划分：

- 数据源：数字、文本、布尔值、当前时间、倒计时/正计时。
- 纯计算：四则运算、函数、比较、逻辑、字符串、统计。
- 图分析：入度/出度、路径、连通区域、循环、拓扑顺序。
- 状态节点：变量、延迟、计数器；它们明确保存跨次运行状态。
- 动作节点：移动/着色/创建节点、提醒等；必须与纯计算分离并受限执行。

参考 Graphif 的公开行为和思想即可，不复制其 GPL-3.0 源码到本 MIT 仓库；实现应基于 Relatum 自有代码独立完成。

## 2. 已确认的产品边界

- 【研究】只有一个“画布”工作方式；界面中不出现“画布/导图/图案”等模式选择按钮。
- 第一阶段复制原画布的相机、节点、连线、选择、撤销、导出、命中检测、批量更新、小地图与 CPU 缓存，再在独立副本中逐步删改；M1 不接入节点 WebGL，GPU 后端只在后续性能门槛触发时评估。
- 研究引擎、样式和数据与原【画布编辑器】隔离；今后删除或重写研究代码不能改变原画布行为。
- 研究画布背景为真正的 CSS 透明：无底色、纸张、网格、渐变、图片、模糊或遮罩。这里指显示父级起步页背景，不等于把整个桌面窗口做成穿透操作系统的透明窗口。
- 离线优先、无 npm/CDN/新前端框架；沿用原生 HTML/CSS/JS 和本地服务。
- 首屏不加载研究大运行时；第一次进入【研究】时按需加载，离开后暂停计时与调度。

说明：原编辑器的普通节点主要是 DOM、静态连线主要是 Canvas2D；`graph-gl.js` 的 WebGL 主要服务关系图几何。目标是继承真实存在的优化，不把“复制 GPU 文件”等同于整张画布已经 GPU 渲染。

## 3. 隔离架构（暂定，可调整）

### 3.1 页面边界

推荐在 `#start-research-workspace` 中按需挂载同源透明 `iframe`，由独立的 `assets/research.html` 承载研究画布。理由：DOM、CSS、全局变量、快捷键和运行循环天然隔离，同时起步页顶栏仍在父页面显示。

拟定目录：

```text
assets/index.html                    # 只负责研究容器
assets/start.js                     # 按需创建/唤醒/暂停研究 iframe
assets/research.html                # 单画布壳，无模式按钮
assets/research/
  research.css                      # 独立透明画布、节点、选择与小地图样式
  research-workspace.js             # iframe 的 activate/suspend/dispose 生命周期桥
  research-editor.js                # 独立内存模型与画布协调
  research-canvas.js                # DOM/Canvas2D/SVG 交互与渲染副本
  research-model.js                 # 无 DOM 内存模型、ID/邻接索引和历史
  research-compute.js               # 无 DOM 计算核心
  research-runtime.js               # 调度、脏传播、动作队列
```

### 3.2 复制原则

- 首次分叉允许大文件直接同源复制，先获得行为/性能基线，再删除不需要的模式和工具。
- 研究副本使用独立文件名、事件前缀、CSS 根类、localStorage 键和全局命名空间，例如 `RelatumResearch.*`。
- 不让研究代码调用或修改 `window.CanvasModule`；不从研究目录反向写原 `canvas.js` / `editor.js`。
- 稳定且无研究特性的 vendor、字体、MathJax、Mermaid 等可只读共享；会被研究改造的运行时代码必须复制。
- 维护一份很短的“共享白名单”；不在白名单中的原编辑器模块不得由研究运行时导入。
- 原画布后续修复不要求自动同步到研究副本；确需移植时人工挑选并分别回归测试。
- 计算、历史和保存只操作研究模型；DOM、Canvas2D 或未来 WebGL 只消费模型投影/变更集合，不得保存第二份业务真源。

### 3.3 数据隔离

- 不复用 `.canvas` 保存链，不把实验字段塞进原画布文件。
- 研究数据暂存于独立命名空间，建议 `data/research-workspace/` 和 `/api/research/*`；后续再决定单工作区还是多项目文件。
- 研究持久数据与运行时分离：节点/连线/配置可保存，当前帧缓存、拓扑索引、计时句柄、计算结果缓存默认不保存。
- Schema 使用独立 `researchVersion`。宣布完工前可以不写迁移器；不兼容时允许显式重置研究数据，但只能影响研究目录，并在执行前说明。
- 后端保存继续使用项目现有原子写入规则。

### 3.4 M0 分叉基线（2026-09-19）

#### 页面、DOM 与初始化

- 原编辑器入口固定为 `assets/editor.html`。研究工作区不直接嵌入这张页面，也不让父级起步页加载它的运行时；M1 从其中复制单画布必需 DOM 到 `assets/research.html`。
- 必需画布壳包括：视口、Canvas2D 静态连线层、参考线层、画布 surface、活动连线 SVG、墨迹层、节点容器契约、绘制/历史工具、搜索、小地图、节点/连线菜单及选择反馈。研究页保留引擎需要的空元素时不得附带说明或占位文字。
- 原协调入口由 `assets/editor.js` 读取文件并调用 `window.CanvasModule.init({...})`，传入视口、surface、各渲染层、工具、搜索、小地图、数据、镜头和变更回调。M1 将启动与生命周期改写到独立的 `research-editor.js`，不得调用原 `CanvasModule`，不得接入 `.canvas` 的加载、保存或 dirty 链。
- 画布视觉当前来自 `assets/styles.css` 的编辑器壳、viewport/surface、节点、连线、工具、菜单、选择、搜索与小地图规则。M1 把实际需要的规则复制进独立 `research.css` 并挂在研究根作用域下；研究页不得直接加载整份 `styles.css`。

#### 原编辑器脚本顺序

以下是 `assets/editor.html` 当前从基础能力到画布协调的加载顺序。研究分叉不得调整这些原入口，也不得把研究脚本插入其中：

```text
i18n.js
desktop-shell.js
sticky-palette.js
mermaid-renderer.js
richtext.js
markdown-table.js
markdown.js
table-editor.js
ruler.js
canvas-import.js
node-matrix.js
canvas-timer.js
markdown-notebook.js
canvas-scenes.js
canvas-taskbook.js
ai-canvas-plan.js
dual-clipboard.js
font-loader.js
canvas-changes.js
canvas.js
editor-lazy.js
editor.js
```

#### 独立复制项与只读共享白名单

- 必须独立复制并改名：`editor.html` 中的单画布 DOM、`canvas.js`、`editor.js` 中的画布启动/生命周期部分，以及对应画布 CSS。复制后使用研究专属根类、事件、偏好键和 `RelatumResearch.*` 命名空间。
- M1 可只读加载的项目模块白名单：`i18n.js`、`sticky-palette.js`、`mermaid-renderer.js`、`richtext.js`、`markdown-table.js`、`markdown.js`、`table-editor.js`、`ruler.js`、`node-matrix.js`、`canvas-timer.js`、`font-loader.js`、`canvas-changes.js`。只有实际保留的功能才加载对应模块；研究特例不得写回这些文件。
- 可只读共享的静态资源：`assets/vendor/` 下的 MathJax、PDF.js、Mermaid、CodeMirror 固定包，`assets/fonts/` 字体和通用图标/图片。仍须保持按真实内容触发，不得为了研究空画布预载重资源。
- M1 暂不接入：`desktop-shell.js`、`canvas-import.js`、`markdown-notebook.js`、`canvas-scenes.js`、`canvas-taskbook.js`、`ai-canvas-plan.js`、`dual-clipboard.js`、`editor-lazy.js`、`editor-onboarding.js`、`ai.js`、`graph-view.js`、`graph-engine.js`、`graph-gl.js`。父页面继续负责桌面窗口；AI、关系图、双屏、导入、任务簿、镜头册和笔记坞不进入第一张研究画布。WebGL 是否接入等 M5 性能数据再决定。
- 研究运行时只允许从白名单读取稳定能力；原 `editor.html`、`editor.js`、`canvas.js` 和 `editor-lazy.js` 永远不得引用 `assets/research/`、`research.html` 或 `RelatumResearch`。

#### 必须继承的性能与降级路径

- 节点与连线维持 `nodeById` / `edgeById` 常驻索引，不在热路径反复线性查找。
- 静态连线保留 Canvas2D 绘制；`edgePathCache` 保存路径、几何、真实中点、点列、边界和叠放顺序，512 世界单位空间网格只查询当前候选。
- 支持 `Path2D/isPointInStroke` 时按空间候选和逆叠放顺序命中；能力不足时保留完整 SVG 命中回退。拖动节点或结构滑行时只临时挂载受影响的 SVG 连线，收尾卸载并更新缓存。
- 批量节点先挂载后统一测量；节点尺寸由单个 `ResizeObserver` 更新真实变化。相机移动只复用缓存几何和变换现有小地图层，不逐边重算、不逐帧扫描全部节点或改写全部小地图样式。
- 抓手和连线光标继续使用独立命中层；低动态偏好关闭非必要过渡。M1 删除功能时可以缩小这些路径，但不得用每帧全图工作替代缓存机制。

#### 原画布回归命令

```powershell
node tests/research-fork-baseline-contract.js
node tests/start-research-shell-contract.js
node tests/research-canvas-baseline-contract.js
node tests/editor-opening-performance-contract.js
node tests/canvas-performance-contract.js
node tests/canvas-changes-regression.js
node tests/clean-style-contract.js
node tests/node-resize-contract.js
node tests/markdown-global-contract.js
```

真实浏览器性能工具保持可选：配置 `RELATUM_PLAYWRIGHT` 后，使用隔离数据根和临时报告路径运行 `node tests/canvas-browser-performance.js <local URL> <isolated runtime root> <report.json> [regression|benchmark|all]`。它不得连接真实用户数据根；未配置 Playwright 时不阻塞仅含清单/契约测试的 M0。

### 3.5 渲染固定决策与 WebGL 门槛

- M1–M4 的默认渲染组合固定为：DOM 节点、Canvas2D 静态连线、只在拖动/滑行等活动手势中临时挂载的 SVG 连线。当前 1200/3000 节点基准已达到既定目标，没有数据支持在 M1 重写节点引擎。
- DOM 保留中文输入法、文本选区、键盘焦点、Markdown/公式/附件和可访问语义。WebGL 更适合大批量简单几何；它不得成为计算、命中、历史或保存的数据真源。
- 现有 `graph-gl.js` 只实例化圆/圆角方块和直线，文字仍由 DOM/Canvas2D 叠加；它只作为未来原型参考，不直接导入研究工作区。
- M5 先用同一浏览器、相同隔离数据根比较 1200、3000 节点基线，再增加 10000 个简单研究节点的概览夹具。既有目标为：1200/3000 冷启动不超过 1.5/3 秒，常规平移/缩放/拖动 P95 不超过 20ms，概览 P95 不超过 33ms，交互收尾不超过 100ms。
- 基线达标即继续 DOM/Canvas2D；只有大规模简单几何不达标时才试验“远处/未编辑简单节点走 WebGL，选中/编辑/富内容节点保留 DOM”的混合后端。只有混合方案仍不达标且节点语义可以完整保留时，才评估全 WebGL，默认不选择全 WebGL。
- 任意 WebGL 候选必须保留 Canvas2D 降级，并覆盖 WebGL2 不可用、着色器失败、上下文丢失/恢复、离页暂停、iframe 销毁、中文输入、文本选择、键盘操作、命中、导出和低动态回归。

## 4. 计算模型

不要把节点标题（例如 `#ADD#`）作为唯一真源。标题可供人阅读，行为使用显式字段：

```js
node.compute = {
  kind: 'value|operator|display|clock|state|action',
  operation: 'add',
  config: {}
};

edge.role = 'visual|data';
edge.fromPort = 'out';
edge.toPort = 'in-1';
```

原则：

- 旧/普通连线默认 `visual`，只有数据连线参与计算。
- 值使用带类型结果：`number|string|boolean|time|nodeRef|error`，错误在节点上可见但不破坏源数据。
- 图结构变化时编译邻接表；输入变化时只标脏并传播到下游，不每帧扫描整张画布。
- 无状态纯计算按拓扑顺序执行；第一版遇到环直接标错。以后只通过明确的 Delay/State 节点允许反馈环。
- 计算结果放在运行时缓存/显示层，不要每帧改写节点正文、历史栈和自动保存。
- Clock/Timer 是主动数据源；仅前台运行。显示刷新可用 RAF，逻辑更新使用真实时间差，不能用“帧数=时间”。
- Action 节点经独立动作队列执行，要求手动启用、限频、校验目标；连续移动只在结束时提交一次历史事务。
- 任意一次执行都应可解释：节点能显示输入、输出、错误和最近运行时间。

## 5. 路线与验收

### M0：冻结原画布基线

- [x] 列出要复制的页面、脚本、样式、静态资源和依赖顺序。
- [x] 记录原画布关键性能路径：ID 索引、连线几何/空间缓存、临时 SVG、批量测量、小地图与降级路径。
- [x] 建立原画布回归命令，保证后续研究改动不会改变它。

验收：只有清单/测试变化，不改变产品行为。

### M1：独立透明画布副本

- [x] 创建透明 `research.html` 和研究目录，按需挂载到起步页；离页暂停、返回激活，页面销毁时释放生命周期。
- [x] 复制并改名核心画布/协调/样式文件，切断全局名、事件、偏好和保存链冲突。
- [x] 固定单画布模式，删除或不渲染模式选择按钮。
- [x] 节点、连线、框选、多选移动、缩放/平移、撤销/重做、小地图可用。
- [x] 离开研究工作区时暂停运行，回来后状态仍在。

验收：研究区可独立操作；背景透明；原画布视觉和功能回归通过。

### M2：独立数据与最小计算闭环

- [ ] 独立 Schema、读取、原子保存、错误恢复。
- [ ] 值、加/减/乘/除、结果节点和数据连线。
- [ ] 手动“运行一次”；缺输入、非数字、除零和循环有节点级提示。
- [ ] 计算核心为无 DOM 模块并有回归测试。

验收：`5 + 6 → 11` 保存重开仍成立；普通视觉连线不参与计算。

### M3：响应式调度与计时

- [ ] 输入变化后的脏节点增量重算。
- [ ] Clock、倒计时、正计时、Delay/Counter 状态节点。
- [ ] 后台/离页暂停，恢复后按真实时间校准；不制造每帧保存和历史记录。
- [ ] 大图调度有预算、取消和性能计数。

### M4：拓扑与动作节点

- [ ] 路径、连通、环、拓扑顺序等分析节点。
- [ ] 受控移动、颜色、通知等动作；默认不自动执行副作用。
- [ ] 一次运行可暂停、单步、停止并查看执行顺序。

### M5：收敛

- [ ] 删除复制后确认无用的旧模式、面板和依赖。
- [ ] 按 3.5 的三级门槛决定继续 DOM/Canvas2D、引入混合 WebGL，或仅在前两者仍不达标时评估全 WebGL。
- [ ] 冻结正式 Schema，之后才开始承诺兼容和迁移。
- [ ] 完成键盘、低动态、深浅主题、桌面壳和打包回归。

## 6. 开发纪律

- 不在原 `canvas.js` 中塞研究特例来图省事；共享抽取必须证明对原画布无行为变化。
- 不先做几十种运算节点；每个阶段先闭合“数据—执行—显示—保存—测试”。
- 不使用 `eval`、`Function` 或直接执行用户 JavaScript。运算来自受控注册表。
- 不用节点空间位置暗中决定输入顺序；使用端口或显式顺序，拖动节点不能改变算式。
- 不允许无界循环、每帧全图计算、每帧写盘或每帧写历史。
- 不把 Graphif 的 GPL 源文件或改写片段复制进本仓库。
- 重要判断写入本文“固定决策”；临时争论、失败尝试和逐次提交记录不要保留。

每个功能的最低验证包括：纯逻辑测试、研究页面交互测试、原画布相关回归；涉及性能时使用大图夹具测量，不凭感觉宣称优化。

## 7. 当前快照（每次覆盖更新）

- 当前阶段：M1 已完成；M2 尚未开始。
- 已确认入口：`assets/index.html` 的 `#start-research-workspace` 初始仍是空 section；第一次进入【研究】时由 `assets/start.js` 创建并复用 `research.html` iframe，不进入首屏或空闲预热链。
- 已确认协调：父页离开研究时调用 `suspend`，返回时调用 `activate`，`pagehide` 或加载失败时调用 `dispose` 并释放 iframe；加载失败安全回到【画布】。研究仍没有后端 API、正式 Schema、保存链或用户数据。
- M1 交互：空白双击或无选择时按 `N` 新建空节点；双击/F2/直接打字编辑；`Tab` 新建带显式端口连线的子节点，`Enter` 新建同级节点，`Alt` 拖动节点创建连线；支持单选、切换多选、框选、多选拖动、Delete、Ctrl/Cmd+A、Ctrl/Cmd+Z/Y、滚轮定点缩放、空格或中键拖动画布、方向键/WASD 平移、Ctrl/Cmd+0/1 和小地图定位。没有可见说明或占位文字。
- 原编辑器入口：`assets/editor.html`、`assets/editor.js`、`assets/canvas.js`。
- 原性能资产：节点/边常驻 ID 索引、Canvas2D 静态连线、`Path2D` 命中、512 世界单位空间网格、几何缓存、受影响连线临时 SVG、批量测量与小地图缓存；`graph-engine.js` / `graph-gl.js` 是关系图的可选 WebGL 路径。
- 渲染决策：M1–M4 保持 DOM 节点 + Canvas2D 静态连线 + 临时 SVG 活动线；WebGL 只按 3.5 的性能门槛评估，当前壳不加载 `graph-gl.js`。
- 分叉边界：研究必须复制画布壳、协调、核心引擎和所需样式；只读共享范围与 M1 排除项已写入 3.4，原编辑器入口禁止反向引用研究代码。
- 已有测试：`tests/start-research-shell-contract.js` 锁定首屏零研究资源、按需 iframe、生命周期和失败回退；`tests/research-canvas-baseline-contract.js` 验证模型/投影边界、ID/邻接索引、512 单位连线网格、显式端口、历史及零 API/存储依赖；`tests/research-fork-baseline-contract.js` 继续锁定原编辑器零反向依赖及关键脚本顺序。
- 本轮变更：新增独立研究模型、协调、画布与样式，闭合 M1 内存交互基线；没有计算节点、保存、后端路由、正式数据格式或用户数据。
- 验证：研究契约及原画布静态/逻辑回归通过；另用本地静态服务在真实 Chromium 中验证中文输入、Tab 建子节点/连线、小地图、适应内容、撤销/重做、离页暂停、返回激活和 iframe 状态复用，控制台无运行错误。当前环境仍未配置 `RELATUM_PLAYWRIGHT`，未执行大图性能测量。

## 8. 当前下一步（只保留一个）

执行 M2 的第一小步：在无 DOM 模块中定义带 `researchVersion` 的独立研究 Schema、带类型值和显式端口校验，并以内存态闭合数字值节点、加法节点、结果节点的 `5 + 6 → 11` 手动运行与错误结果测试。暂不增加保存/API/用户数据或可见说明文字；渲染层只消费计算投影，不成为第二份数据真源。
