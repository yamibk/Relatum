# Relatum「研究」工作区接力开发

> 状态：M5「V3 可复用子电路」、M5.1「前端操作统一与面板优化」、M5.2「视口手感与小地图」、M5.3「多章节教程与可生成实例」、M5.4「选区整体复制」、分层 CPU 与 1,000 个主动时间节点验收已完成；下一步量化复制对大型 CPU 搭建操作量的改善，再决定是否需要跨页剪贴板。显式图分析延期到出现具体使用场景后。最后校准：2026-09-22。
> 新对话提示词：`读《研究工作区接力开发.md》，继续开发并更新进度。`

## 1. 目标与边界

研究工作区是与原画布、笔记、学习系统隔离的可执行无限画布。当前目标不是堆叠大量专用按钮，而是用少量可组合的值、逻辑、事件和状态节点，搭建数电、红石式逻辑、计时工具和后续 CPU 数据通路。

固定边界：

- 独立同源透明 iframe，首次进入研究时按需加载；离开、页面隐藏和销毁时停止调度。
- 页面彼此独立，不支持跨页导线；子电路只通过显式类型化端口连接页面。
- 不加入网络、脚本执行、通知、移动节点等副作用能力。
- 不修改 `.canvas`、笔记、学习或其他用户数据格式。
- 研究 V1/V2 已废弃且不迁移。读取非 V3 研究文档时返回空白 V3，首次保存覆盖研究文件，不生成旧格式备份。
- 不使用 `eval`、`Function` 或用户 JavaScript；执行函数只来自受控前端注册表。

## 2. 文件与职责

```text
assets/research.html                         # 研究页面壳、可折叠底栏、左侧统一面板、教程容器
assets/research/research-workspace.js        # iframe activate / suspend / dispose 桥
assets/research/research-editor.js           # 页面、模型、执行、仿真、面板与保存协调
assets/research/research-canvas.js           # DOM 节点、端口、两种连线、Canvas/SVG、小地图
assets/research/research-model.js            # V3 节点/边、索引、历史、建线即时校验
assets/research/research-tutorial.js         # 教程目录/翻页/焦点/进度和实例插入协调
assets/research/research-tutorial-content.js # 39 页、4 章中文教程内容
assets/research/research-tutorial-examples.js# 8 套本地确定性节点群模板
assets/research/research-pages.js            # 独立分页、镜头、速度、逐页运行态
assets/research/research-node-definitions.json # 前后端共用的声明式节点/端口注册表
assets/research/research-registry.js         # 前端注册表、配置和值规范化
assets/research/research-schema.js           # researchVersion:3 文档规范化与校验
assets/research/research-subcircuits.js      # 定义目录、选区封装、修订/依赖和递归展开
assets/research/research-values.js           # typed value / Bits 工具
assets/research/research-compute.js          # 拓扑编译、增量传播、事件批次与状态提交
assets/research/research-runtime.js          # Run/Pause/Step/Reset、倍率、可见性调度
assets/research/research-persistence.js      # 独立 GET/POST 传输层
assets/research/research.css                 # 研究区独立视觉
research_store.py                            # Python V3 校验、修订、原子保存、损坏恢复
```

原编辑器不得反向引用 `assets/research/`。研究区继续使用 DOM 节点 + Canvas2D 静态线 + 手势期间 SVG 活动线；保留 ID 索引、512 世界单位空间网格、几何缓存和小地图 DOM 索引，不加载 WebGL。

## 3. V3 持久模型

```js
{
  researchVersion: 3,
  subcircuits: [{
    id, name, latestRevision,
    revisions: [{
      revision,
      ports: [{ id, name, direction, channel, valueType, bitsWidth?, nodeId, portId }],
      nodes, edges
    }]
  }],
  pages: [{
    id, title,
    nodes: [{
      id, type, label, x, y, width, height,
      config,
      statePolicy: "reset" | "persist",
      savedState? // 普通 persist 状态，或子电路实例的递归状态树
    }],
    edges: [
      { id, kind: "relation", fromNodeId, toNodeId },
      { id, kind: "wire", from: { nodeId, portId }, to: { nodeId, portId } }
    ],
    view: { x, y, scale },
    simulation: { speed }
  }],
  activePageId
}
```

- `relation` 只表达知识关系，永不进入执行图。
- `subcircuit` 实例只保存 `config:{definitionId,revision}`；动态端口来自固定修订，不在实例复制。
- `wire` 只能连接注册表声明的输出端口和输入端口。
- 值输入最多一条线，事件输入可接多个来源，输出可任意扇出。
- 建线时校验方向、value/event 通道、声明类型、已知 Bits 位宽、同组输入位宽和占用；非法线不进入模型或历史。服务端保存时再次校验。
- 节点标题只是 `label`，编辑标题不会更改计算配置。
- 事件队列、投影、拓扑缓存、调度句柄、性能计数和撤销历史不保存。

## 4. 值、事件与执行语义

稳定值类型：

- `number`：有限浮点数。
- `boolean`。
- `string`。
- `time`：毫秒时间戳。
- `bits`：1–64 位无符号位向量，保存为 `{type:"bits", width, value:"0x..."}`；数学和位运算按位宽模 `2^N` 回绕。

Pulse 是只存在于运行时的瞬时事件，带全局递增序号和仿真时间，不写文档。类型转换必须经过 Convert；不做隐式字符串、数字或位宽转换。

确定性规则：

1. 普通值修改后，沿出站索引自动增量传播。
2. 组合值图按拓扑序求值；不经过状态边界的组合环报错。
3. 同一仿真时刻的 Pulse 组成一个批次。先读取旧状态并暂存所有状态更新，再统一提交并传播，因此多个 Register 同步采样。
4. 事件按仿真时间、递增序号和连线创建顺序稳定分发。
5. 每批最多 1,000 个 Pulse、10,000 次节点求值；超限后运行时暂停并给出可定位错误。
6. 每页持有独立编译计划、事件序列、状态、投影和统计，切页不串状态。

## 5. 节点族

声明式注册表 `research-node-definitions.json` 是前端建节点、画端口、属性控件、Schema 校验以及 Python 端口白名单的共同来源。

| 类别 | 节点 | 语义 |
| --- | --- | --- |
| 知识 | Note | 只参与关系线 |
| 输入 | Constant | 配置 number/boolean/string/time/Bits 常量 |
| 输入 | Button | 每次点击立即产生一个 Pulse，暂停时也执行完整批次 |
| 输入 | Toggle | 交互式稳定 boolean |
| 输入 | Current Time | 墙上时间，不受仿真倍率影响 |
| 输入 | Clock | 按可控仿真时间周期输出 Pulse |
| 组合 | Math | number 或同位宽 Bits 的加减乘除与取模 |
| 组合 | Logic | boolean 或同位宽 Bits 的 AND/OR/XOR/NOT |
| 组合 | Compare | 同类型、同位宽比较，输出 boolean |
| 组合 | Select | boolean 条件选择两个同类型、同位宽值 |
| 组合 | Convert | 显式类型/位宽转换 |
| 组合 | Bits | 逻辑移位、Concat、Slice、Resize |
| 状态 | Register | `write` 时同步采样 `data`；`reset` 恢复初值 |
| 状态 | Counter | `inc/dec/reset/load`，number/Bits，可回绕或饱和 |
| 状态 | Edge Detector | boolean 或 Bits<1> 的 rise/fall/change Pulse |
| 状态 | Timer | 统一正/倒计时；start/pause/reset/preset，输出 time/running/done |
| 显示 | Monitor | 长期醒目显示任意值，取代旧“结果”节点 |
| 显示 | Probe | `value` 观察值变化，`pulse` 观察多路事件；轨迹只存在于运行时 |
| 显示 | Lamp | 显示 boolean 或 Bits 状态 |
| 模块 | Subcircuit | 固定定义修订的动态端口实例；编译时递归展开，实例状态完全隔离 |

旧版单独的加、减、乘、除、倒计时、正计时、Delay、Result 一级工具均已移除。

## 6. 仿真时间与状态保存

底部运行栏提供运行、暂停、单步、复位，以及 `0.25× / 0.5× / 1× / 2× / 4×`。

- 冷启动默认暂停；组合逻辑无论暂停与否都会自动响应。
- 单步推进到下一个 Clock/Timer 边界，并排空该时刻事件批次。
- 离开研究或页面隐藏时冻结；返回后恢复离开前的运行意图，不累计隐藏或应用关闭期间的时间。
- Button 在暂停时仍立即执行。
- Current Time 使用墙上时间；Clock 和 Timer 使用页面独立的仿真时间。
- 状态默认 `reset`。用户在属性面板对单节点启用“重开后保留状态”后才保存其离散状态。
- Timer 保存已结算进度和运行意图；重开从该进度继续。
- 全局复位无视保留策略，恢复所有初值并让后续保存更新 `savedState`。

## 7. 交互

- 左侧统一面板首次默认展开，空选时显示搜索框、全部内建节点与全部可复用子电路；选中对象后原位切换属性或选区摘要。节点库与各类属性内容共用固定 `232px × 635px` 外框（窄窗按可用高度钳制），对象或内容类型变化时立即替换，不保留旧层退场动画；面板、控件与模态层收敛为简洁黑白表面，去除装饰性灰底、渐变和多余分隔线。裸按 `Tab` 或面板关闭按钮开合，偏好保存为 `research:sidePanelCollapsed:v1`；输入、正文编辑、IME、模态框与面板内部键盘导航不劫持 `Tab`。
- 当前创建项是内建节点类型或 `{definitionId,revision}` 子电路描述符。两者都只在面板中单击选中，再双击稳定画布空白放置；无效或已删除的子电路选择回退到 Note。浅色主题用黑色块、深色主题用暖白色块标记当前项。
- 双击节点主体或标题只进入标签行内编辑；仅连续两次都命中稳定空白才创建一个当前项。第一次点击节点引起面板内容切换时，面板在双击判定窗口内暂停命中，避免覆盖第二次点击。
- 单节点属性和多选摘要都提供复制入口，`Ctrl/Cmd+D` 也可复制当前节点选区。副本保留标签、配置、尺寸、状态策略与固定子电路修订，只复制两端都在选区内的关系线/导线，并清除整棵 `savedState`；选区外连线不复制。副本组选中并以最近一次画布内鼠标位置作为包围盒左上角；会话尚无鼠标位置时整体右下偏移 28px。一次复制只写一条历史，可整组撤销/重做；不提供系统剪贴板、跨页或跨会话粘贴。
- 关系线和导线是 Alt 手势的默认连接类型，不再是互斥指针工具；偏好保存为 `research:connectionKind:v1`，首次默认导线。普通左键始终选择、拖动或触发节点按钮。
- 关系线按住 Alt 从节点主体拖到另一节点主体，并裁切到两端真实可见边框；端口不算关系线主体。导线必须按住 Alt 从真实输入或输出端口拖到兼容端口，反向拖拽会自动整理为持久模型要求的输出到输入。手势开始后释放 Alt 不取消本次连线。
- 导线拖到具体非法端口时只把活动预览线变红，松手后拒绝，不显示错误文字，也不写入模型或历史；空白处松手直接取消。
- 兼容、未占用的候选端口继续高亮。从已占用的单连接值输入主动拖出时，合法落到新输出会保留 edge ID 并以一条历史原子重接；取消或非法落点保留旧线。事件输入仍新增来源，从输出拖到已占用值输入仍拒绝。节点选中、端口悬停或正在连线时显示端口名和类型。
- 单节点属性按基本信息、节点配置、状态、运行轨迹/子电路修订分组，保留真实配置、Probe 清空和兼容升级；多节点显示摘要与“封装为子电路”；单连线显示类型、两端与删除入口。选区变化只切内容，不替用户展开已收起的面板。
- 底栏展开态固定为 `540px × 44px`，窄窗钳制并允许内部横向滚动；收起时只保留侧边箭头，偏好保存为 `research:computeDockCollapsed:v1`。选择、运行状态和按钮显隐不改变外框尺寸。
- 研究相机沿用主画布的成熟参数：滚轮以指针为锚点写入目标镜头并用按帧时长归一化的 RAF 缓动，空格/中键拖动即时跟手、松手按 EMA 速度和时间无关摩擦滑停，方向键/WASD 连续平移同样按帧时长归一化。低动态偏好下缩放即时到达且禁用松手惯性；切页、挂起、失焦和新交互必须清理冲突中的相机任务。
- 右下角研究设置只保存本机偏好：`research:panSpeed:v1`（1–20，默认 8）、`research:panInertia:v1`（0–1，默认 0.15）和 `research:zoomSpeed:v1`（0.5–3，默认 1），不得读取或改写主画布的 `canvas:*` 对应键。设置与教程浮层互斥，恢复默认只清除这三个研究键。
- 右下角“？”或 `?` 快捷键按需打开 39 页、4 章中文教程，不首次自动弹出。目录跳转、左右键、前后页、Esc、焦点圈定/归还均由 `research-tutorial.js` 管理；最后阅读页仅写 `research:tutorialProgress:v1`，不写研究文档。窄窗口使用全屏单栏，目录与正文隐藏原生滚动条但保留滚轮/触控。
- 8 个实战页可生成本地模板：全部节点/端口/导线先经真实注册表整批校验，ID 全量重映射，再由 `ResearchModel.insertGraph()` 一次提交到当前页。空位以当前视口中心确定性搜索，失败不改模型/历史/镜头；成功关闭教程、整组选中并聚焦，一次撤销/重做移除/恢复整组。这条链不调 AI、不联网、不新建页或子电路定义，不改倍率和后端接口。
- 左下角显示缩放比例与 `180px × 120px` 小地图。小地图映射包含节点和当前视口；节点缩略块使用常驻 DOM 索引，相机移动只变换缩略层并移动最小 4px 的取景框，停止后再校准，不能逐帧重建节点。节点拖动只更新受影响缩略块；点击框外平滑居中，拖动取景框保留抓取偏移并即时平移。
- Button/Toggle 在节点本体有明确交互按钮；节点显示简短输出、Pulse、Timer 运行状态和真实错误。
- Probe 通过导线显式选择观察对象：一个可选 `value` 输入和可多来源的 `pulse` 输入。选中 Probe 时，属性面板按新到旧显示仿真时间、Pulse 序号、来源节点/端口与值；上限 16–256，默认 64。
- Probe 只在值真正变化时记值，每个 Pulse 均记录。暂停不清空，手动 Button 仍可在当前仿真时间留下记录；单步完整记录下一批；全局复位清空并重建 `t=0` 值基线。面板“清空”不改电路、不写撤销历史。
- 右侧页栏、镜头、选择、历史、小地图和按需 iframe 生命周期继续保留。页栏由窗口最右侧 32px 热区显式唤出；不得把悬停监听只绑在 `visibility:hidden` 的页栏本体上。

### 子电路交互与修订

- 选中一个或多个节点后，左侧统一面板显示“封装为子电路”。跨界 wire 按内部端点自动生成类型化输入/输出端口，名称和无法静态推导的值类型在确认面板中可编辑；内部实时 `savedState` 不进入定义。
- 可新建定义，也可把另一组选区发布为现有定义的不可变新修订。方向、通道、具体类型和顺序兼容的端口复用稳定 ID；页面替换只写一次历史，撤销恢复原选区但保留已发布定义。
- 左侧节点列表列出每个定义的最新修订。旧实例固定原修订；属性面板仅在所有已连接端口仍兼容时提供手动升级，升级会清空旧实例状态。右键列表中的定义可删除未被任何页面或定义引用的最新修订。
- 定义可包含固定修订的其他实例，依赖图必须无环且不超过 32 层。运行时按实例路径展开并聚合外层端口投影；状态、Pulse、Probe 与持久状态树不会在实例间共享。

## 8. 持久化与安全

- 唯一路由是 `GET/POST /api/research/workspace`；文件只在 `data/research-workspace/`。
- 保存复用项目原子 JSON 写入和 SHA-256 修订冲突保护。
- V3 有效主文件保存前写 `workspace.backup.json`；损坏主文件隔离为 `workspace.corrupt-*.json` 后从有效备份或空文档恢复。
- V1/V2 不进入备份，不迁移；其他用户数据完全不受影响。
- 350ms 合并保存。持久状态只随离散状态变化、暂停/切页/离页或正常保存落盘，不随显示刷新逐帧写盘。

## 9. 路线

### M0–M3：旧原型（完成，随后由 V2 取代）

- [x] 独立透明 iframe、画布分叉、分页、保存、最小数据流、旧时间源和旧状态原型。

### M4：V2 可组合运行时（完成）

- [x] `researchVersion:2` 与 relation/wire 联合类型。
- [x] 单一声明式节点注册表和严格 typed value / Bits。
- [x] 自动组合传播、状态边界、同步 Pulse 批处理与安全预算。
- [x] Register、Counter、Edge Detector、统一 Timer。
- [x] 运行/暂停/单步/复位/倍率和可见性生命周期。
- [x] 搜索添加面板、独立连线模式、属性面板、Monitor/Lamp。
- [x] 单节点状态保留和 V1 直接淘汰策略。
- [x] 四页浏览器验收夹具：全加器、8 位 ALU、4 位同步累加器/PC、Timer `done` 驱动 Counter。
- [x] Probe 有界值/事件轨迹，覆盖暂停、单步、复位、清空与非持久化语义。

### M4.1：临时连线与端口交互优化（完成）

- [x] 关系线按节点边框裁切，导线跟随真实 DOM 高度和端口中心。
- [x] 所有输入/输出端口均可起线，反向手势自动规范方向。
- [x] 非法候选仅显示红色活动预览；已占用值输入支持保留 edge ID 的原子重接。

### M5：V3 可复用子电路（完成）

- [x] `researchVersion:3`、不可变定义修订和实例动态端口；V1/V2 直接淘汰。
- [x] 从选区按跨界 wire 封装；确认面板要求补齐未知值类型，并可把未匹配端口映射回兼容旧端口 ID；页面替换原子提交，添加面板可复用。
- [x] 固定修订、兼容性检查后的手动升级、未引用修订删除和定义引用保护。
- [x] 最多 32 层无环嵌套、递归编译、实例路径命名空间与展开预算。
- [x] 组合值/Pulse 穿透、外层投影聚合、实例状态与递归持久状态树隔离。
- [x] 真实 Edge 公开交互验收：新建定义、发布 r2、旧实例固定、手动升级、复用与刷新恢复。
- [x] 从空白 V3 文档经公开 UI 搭建 4 位累加器 CPU；ROM/Core r1→r2、Probe 顺序、状态清空、25 Core/1,006 展开节点与冷重载均通过。

### M5.1：前端操作统一与面板优化（完成）

- [x] 添加列表与对象属性合并为左侧统一面板；首次展开，`Tab`/关闭按钮开合并记忆，深色面板使用高不透明度墨绿黑表面且不持续模糊。
- [x] 节点库、单选/多选和连线属性共用固定尺寸容器；对象切换保留短促出入场，帮助与子电路模态层补齐有限退场，控件反馈统一并提供低动态静态降级。
- [x] 面板、底栏控件和模态层视觉收敛为黑白主色，去除装饰性渐变、灰底块和非必要分隔线；深色节点使用与浅色黑线对称的暖白细描边，仅保留错误与连线兼容等必要语义提示。
- [x] 内建节点和子电路统一为“单击选中、双击稳定空白放置”；节点双击只编辑标签，已删除创建项自动回退 Note。
- [x] 关系线/导线改为记忆的默认连接类型；仅 Alt+左键建线，关系线严格主体到主体，导线严格真实端口到兼容端口，普通拖动始终移动节点。
- [x] 单节点、选区和连线检查器统一进同一面板；“封装为子电路”从底栏移入选区面板。
- [x] 底栏固定 `540px × 44px`，窄窗内部滚动，并提供只保留侧边箭头的记忆式收起。
- [x] 静态契约和真实浏览器夹具同步覆盖 Alt 连线、子电路双击放置、Tab/折叠状态、固定底栏及节点/空白双击竞争。

### M5.2：视口手感与小地图（完成）

- [x] 定向移植主画布的目标/当前双状态相机、帧率归一化缩放、指针锚定、拖拽测速/惯性和连续键盘平移，不反向改动主画布热路径。
- [x] 增加独立研究设置齿轮、三项 `research:*` 本机偏好、恢复默认、外点/Esc 关闭、帮助互斥及深浅色/低动态表现。
- [x] 小地图升级为包含视口的缓存映射、常驻节点 DOM、相机期间图层变换、停止后校准、点击平滑跳转和取景框跟手拖动；缩放比例按钮可恢复 100%。
- [x] 静态契约与真实 Edge 夹具覆盖平滑锚定缩放、惯性开关、键盘速度、小地图 DOM 复用、跳转、刷新恢复、偏好隔离和低动态降级。

### M5.3：多章节教程与可生成实例（完成）

- [x] 把单页说明升级为 39 页/4 章：入门 6、节点 20、实战 8、进阶 5；20 种注册节点各有且仅有一页。
- [x] 目录、前后翻页、左右键、Esc、焦点管理、最后阅读页、窄窗口全屏单栏、深浅色和低动态降级完成；不自动弹出，不使用持续 blur。
- [x] 价格折扣、截止提醒、点击计数、变化记录、番茄计时、全加器、8 位 ALU、4 位累加器/程序计数器共 8 套模板通过真实注册表和端口校验。
- [x] `insertGraph()` 在临时候选模型中预校验全部节点/边，重映射 ID 后一次写历史；非法模板零写入，合法模板一次撤销/重做整组恢复。
- [x] `tests/research-tutorial-regression.js` 静态校验页数/映射，并对 8 套模板做真实模型插入、ID、历史、撤销/重做和非法整批回滚回归。

### M5.4：选区整体复制（完成）

- [x] `ResearchModel.duplicateNodes()` 复用 `insertGraph()` 原子复制节点与内部关系线/导线，全量重映射 ID，跨界连线不复制。
- [x] 副本保留配置、尺寸、状态策略和固定子电路修订，清除普通状态与递归子电路 `savedState`，从初始状态运行。
- [x] 单节点属性、多选摘要和 `Ctrl/Cmd+D` 共用同一入口；副本以最近画布鼠标位置为锚并成为新选区，无锚点时使用 28px 偏移。
- [x] 模型回归覆盖内部两类连线、跨界排除、状态清除、空选择、一次历史及撤销/重做；真实 Edge 验收覆盖鼠标落点、选区切换、面板按钮、保存和原子历史。
- [x] 教程示例的 i18n 访问改用 `globalThis` 安全探测，恢复 Node 教程回归；浏览器夹具同步移除已废弃的面板退场层断言并校准真实 `540px × 44px` 底栏。

### 后续：显式图分析（延期）

- [ ] 等出现具体分析工作流后，再设计 Graph Snapshot、节点引用和显式子图输入；当前不为抽象能力扩充 V3 值协议。
- [ ] 再加入路径、连通、环、拓扑顺序等纯分析节点。
- [ ] 分析节点不得偷偷读取整页；必须显式选择关系线、导线或指定子图。

### 后续：大型电路与动作（进行中）

- [x] 以真实分层 CPU/大型逻辑搭建体验评估子电路、模块实例与调试探针。
- [x] 完成 1,000 个主动时间节点的真实浏览器分片报告，并按实测把连续仿真投影改为合并、逐帧限量刷新。
- [ ] 受控移动、着色、通知等副作用节点继续暂缓。
- [ ] 达到大规模瓶颈后才按既有性能门槛评估混合 WebGL。

## 10. 验证

当前测试：

```powershell
node tests/start-research-shell-contract.js
node tests/research-canvas-baseline-contract.js
node tests/research-model-regression.js
node tests/research-tutorial-regression.js
node tests/research-pages-regression.js
node tests/research-compute-regression.js
node tests/research-subcircuits-regression.js
node tests/research-runtime-regression.js
node tests/research-persistence-contract.js
python -m unittest tests.test_research_store -v
node tests/research-fork-baseline-contract.js
```

可选真实 Edge 验收：

```powershell
node tests/research-circuit-browser.js <本地 URL>
node tests/research-subcircuits-browser.js <本地 URL>
node tests/research-cpu-browser.js <本地 URL> [report.json]
node tests/research-time-slicing-browser.js <本地 URL> [report.json]
```

已覆盖 V3 Schema、共享注册表、端口方向/通道/基数、Bits 1/8/16/32/64 位规范化与回绕、Math/Logic/Compare/Select、组合环、Register 状态边界、同步多寄存器采样、Timer→Counter、持久状态、运行栏、倍率、页面隔离、隐藏暂停、过期回调、V1/V2 丢弃、已发布修订不可改写、原子保存、冲突和损坏恢复。大图纯逻辑回归使用 1,500 级链验证迭代执行。

画布基线还覆盖关系线水平/垂直/对角边框交点、重接保持 edge ID、单步撤销/重做、非法重接模型不变、统一左侧面板、连接类型偏好、两个折叠入口、独立视口偏好和相机/小地图生命周期。选区复制模型回归另覆盖内部关系线/导线、跨界排除、ID 重映射、配置与修订保留、状态清除、28px 回退、空选择和单条历史。真实浏览器验收另确认普通拖动不建线、Alt 输入端反向拖线、占用冲突拒绝、动态高度端口对齐、Alt 关系线边框落点、Tab 面板开合、节点双击只编辑、子电路双击空白放置、底栏尺寸恒定、选区复制的鼠标锚点/新选区/面板按钮/保存/撤销重做，以及平滑锚定缩放、拖拽惯性/关闭惯性、键盘平移速度、小地图 DOM 复用与点击跳转、偏好刷新恢复/隔离和低动态降级；验收服务必须继续使用隔离的临时研究存储，不能改写真实 `data/research-workspace/`。

`tests/research-circuit-fixtures.js` 将四个验收电路放在四个独立研究页；`tests/research-circuit-browser.js` 通过真实 Edge 和页面公开交互验收它们，包括属性面板增量传播、右缘悬停页栏、Run/Pause/Step/Reset、Probe 的 `t=40ms` 同批 Pulse/值顺序与清空/复位、Button 暂停/重启 Timer 及 `done` Pulse。该夹具曾实际发现并固化“全局正在运行时，手动重启 Timer 却未重新排程”的回归。

浏览器夹具的搭建摩擦结论：

- 全加器和小型 ALU 已能用公开节点组合，稳定值用 Monitor 足够检查。
- 时序图的主要困难不是“算不出来”，而是看不到 Pulse 的时间、先后和状态变化历史；单个节点的 `Pulse #N` 不足以调试。
- 重复配置和重复布线会在更大电路中很快放大，但当前四个验收电路尚未被缺少子电路所阻断。

`tests/research-subcircuits-regression.js` 另覆盖单层/嵌套组合传播、Pulse 输入、两个实例状态隔离、递归持久状态、选区边界端口、edge ID 保留、一次撤销、修订固定、升级兼容性、引用删除和循环拒绝。`tests/research-subcircuits-browser.js` 通过真实 Edge 与公开 UI 验收新建定义、发布 r2、旧实例保持 r1、手动升级、先选子电路再双击空白复用和刷新恢复；与原四页浏览器夹具一样拦截研究接口，不改真实研究数据。

`tests/research-cpu-browser.js` 从空白文档开始，只走左侧统一面板、双击放置、属性面板、Alt 端口拖线、多选封装、修订发布、实例升级、运行栏和 Probe。2026-09-21 的实测中，r1 在 8 步后、r2 在 12 步后稳定于 `PC=3、ACC=0、HLT`；旧 Core 实例保持 r1，手动升级清空状态并运行 r2。25 个 Core r2 展开为 1,006 个节点、1,356 根导线，最终快照通过 V3 Schema/编译校验，五轮单步与三次冷重载无类型、预算、控制台或页面错误，运行态轨迹没有持久化。完整环境、操作计数与非门槛性能数据见 `docs/research-cpu-evaluation-2026-09-21.md`。

CPU 工作流共记录 651 次公开操作，其中属性配置 202 次、拖线 173 次、端口命名 33 次。重复配置和重新组合修订是明确摩擦，但没有阻断搭建；Probe 足以确认 Pulse 与 ACC 的真实变化顺序，因此本轮不增加专用定义编辑器、批量模块操作或渲染后端。

`tests/research-time-slicing-browser.js` 使用一次性 V3 文档在真实 Edge 中运行 1,000 个同步 16 ms Clock，验证每个同刻 1,000 Pulse 批次完整提交、每批四个最多 256 源的计算片段、suspend/activate/公开暂停和运行计数非持久化。首次基线确认计算分片有效，但同步刷新 1,000 个节点使 RAF 帧间隔中位达到 82.9 ms；连续仿真的投影展示现改为合并最新结果并按每帧最多 128 个节点、最多 4 ms 分片，单步与复位仍同步。复测 37 个完整批次、37,000 Pulse，观察到 114 次协作让出，帧间隔中位 16.7 ms、P95 17 ms；仍保留一次 55 ms Long Task，不宣称零卡顿，也不据此引入 WebGL。完整环境与前后数据见 `docs/research-time-slicing-evaluation-2026-09-21.md`。

## 11. 当前下一步（只保留一个）

用现有分层 CPU 公开工作流重测选区复制实际减少的节点放置、属性配置和重复布线操作量；只有结果显示仍需要跨页复用时，才设计受控的跨页剪贴板。显式 Graph Snapshot 继续延期，直到有具体分析场景能够定义输入、输出和失效语义。
